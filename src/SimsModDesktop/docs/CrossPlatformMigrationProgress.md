# 跨平台迁移进展（截至 2026-03-03）

## 1. 已完成

### 1.1 架构层
* 桌面主执行链路已完成 .NET 分层化：Presentation -> Application -> Infrastructure。
* 历史脚本依赖已清理，桌面端主执行流程不再依赖 PowerShell。
* 架构守卫测试已落地，防止旧组件回流。

### 1.2 能力层
* 统一文件转换引擎覆盖 `flatten/normalize/merge/organize`。
* `find duplicates` 已支持应用内哈希分组、导出与清理流程。
* Tray 依赖分析、Tray 预览、Save 导出、Package 解析、纹理处理均已模块化。

### 1.3 基础设施层
* 跨平台文件服务、哈希服务、配置服务已标准化注入。
* SQLite 缓存/索引用于托盘与 Mod 相关数据通路。

---

## 2. 进行中

1. 用例层收敛：已移除 `NoOp*UseCase` 占位实现与 DI 注册，新增守卫测试防止回流。
2. 桌面链路内部对齐：继续减少功能分支间的参数语义偏差。
3. 跨平台回归覆盖扩充：重点补齐高负载 I/O 和边界路径测试。

---

## 3. 下一阶段

1. 以用户高频场景为优先，完成执行/预览/索引通路的用例化封装。
2. 统一结果对象与状态上报，降低字符串状态耦合。
3. 形成稳定的跨平台发布前核验清单（构建、测试、性能、回归样本）。

---

## 4. 当前结论

迁移工作已完成“架构落地”和“核心能力迁移”，进入“行为收敛与工程化完善”阶段。  
现阶段主要风险不在功能缺失，而在双链路长期并行带来的一致性与维护成本。
